# Ignore Firebase service account key and other sensitive files
serviceAccountKey.json
serviceAccount*.json
serviceAccount.json
drive-key.json
*.pem
*.key
*.log

# Node / env
node_modules/
.env

# Sensitive assets (do not push)
# asset/vd/ — décommenté pour permettre le déploiement de la vidéo de présentation sur Vercel
